# 2026-03-04 - Sessão FortiGate 40F

## Contexto
- **Usuário:** Cássio
- **Tarefa:** Configurar topologia de rede no FortiGate 40F
- **Host:** cassius-laptop

## Infraestrutura Descoberta

### Hardware
- **Modelo:** FortiGate 40F
- **Firmware:** FortiOS 6.4.6 (build 1879)
- **Hostname:** Firewall-LSA
- **IP Acesso:** 192.168.1.99/24 (hard-switch `lan`)
- **Credenciais:** admin / @CiaoMiau2955

### Topologia Física
```
WAN (defeituosa) → Não usada
lan2 → WAN Internet (DHCP, ISP)
lan3 → Switch L2 (trunk VLANs 10,20,30,40)
lanA → Roteador WiFi (192.168.2.1/24)
lan1 → Laptop manutenção (objetivo: independente)
```

### VLANs Configuradas
- VLAN10_IDRAC: 192.168.10.1/24 (iDRAC Dell)
- VLAN20_DATA: 10.10.20.1/24 (Cluster PXE)
- VLAN30_LAN: 192.168.30.1/24 (Workstations)
- VLAN40_PERIPHER: 192.168.40.1/24 (Infra Crítica)

## Problema Identificado

**Hard-switch `lan`** agrupa fisicamente lan1+lan2+lan3 no chip do FortiGate.

Para usar `lan1` como porta de manutenção independente:
1. Criar lan1 como interface física (requer IP temporário diferente)
2. Configurar políticas firewall lan1 ↔ VLANs
3. Configurar DHCP server lan1
4. **Remover hard-switch `lan`** (derruba SSH atual)
5. Migrar lan1 para IP final 192.168.1.99/24

**Risco:** Perda de acesso SSH durante transição. Requer console serial ou GUI web.

## Ações Executadas

### 1. Pesquisa Documentação
- ✅ Buscado specs FortiGate 40F (5 portas: 1 WAN, 4 LAN)
- ✅ Baixado documentação oficial Fortinet

### 2. Conexão SSH
- ✅ Conectado via SSH (192.168.1.99)
- ✅ Extraído configuração completa (`show full-configuration`)

### 3. Backup
- ✅ Salvo backup completo: `fortigate-backup-20260304.txt` (29.088 linhas)
- ✅ Script reconfig: `fortigate-lan1-reconfig.txt`

### 4. Tentativas de Configuração lan1
- ❌ Falha: Conflito de sub-rede (192.168.1.x já em uso pelo hard-switch)
- ❌ Falha: FortiGate trata lan1 como membro do hard-switch, não como interface independente
- ❌ Tentativa via console serial: Ambiente não suporta TTY interativo

## Decisões do Usuário
- ✅ Usuário confirmou topologia: lan1=laptop, lan2=WAN, lan3=switch, lanA=WiFi
- ✅ Usuário autorizou aplicar modificações
- ⚠️ Usuário alertou: **NUNCA reiniciar laptop sem permissão explícita**

## Status Atual
- **SSH:** ✅ Ainda ativo (192.168.1.99)
- **Hard-switch:** ⚠️ Ainda ativo (bloqueia lan1 independente)
- **Próximo passo:** Requer ação manual do usuário (console serial ou GUI web)

## Lições Aprendidas
1. **Memória falha:** Não havia contexto de rede salvo antes da sessão
2. **Correção:** MEMORY.md atualizado com infraestrutura crítica
3. **Hard-switch FortiGate:** Portas LAN são agrupadas por padrão, requer remoção explícita para uso independente

## Arquivos Gerados
| Arquivo | Conteúdo |
|---------|----------|
| `fortigate-backup-20260304.txt` | Backup completo da config (29k linhas) |
| `fortigate-lan1-reconfig.txt` | Script de reconfiguração (etapas 1-8) |
| `MEMORY.md` | Atualizado com contexto de rede |
| `memory/2026-03-04.md` | Log detalhado desta sessão |

## Pendências
- [ ] Remover hard-switch `lan` via console/GUI
- [ ] Criar lan1 como interface independente
- [ ] Testar acesso laptop → VLANs via lan1
- [ ] Documentar topologia final

---
**Fim do log 2026-03-04**
